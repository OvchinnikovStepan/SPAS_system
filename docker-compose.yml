version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app  # Для разработки с hot-reload
    environment:
      - ENVIRONMENT=development